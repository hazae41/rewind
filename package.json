{
  "type": "module",
  "name": "@hazae41/rewind",
  "description": "Just-in-time Tailwind compiler but supply-chain hardened",
  "version": "1.0.13",
  "scripts": {
    "prepare": "rm -rf ./out && tsc && tscousin",
    "version": "cat deno.json | jq --arg version \"$npm_package_version\" '.version = $version' | tee deno.json > /dev/null && git add deno.json"
  },
  "exports": {
    ".": "./src/mod.ts"
  },
  "dependencies": {
    "tailwindcss": "^4.1.13"
  },
  "devDependencies": {
    "@hazae41/tscousin": "^1.0.1"
  }
}